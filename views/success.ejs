<!DOCTYPE html>
<html>
<head>
  <title>URL Shortened</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      /* background: linear-gradient(135deg, rgba(112, 141, 196, 0.7) 0%,rgba(112, 141, 196, 0.7) 100%); */
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Montserrat', sans-serif;
    }
    .success-card {
      background: rgba(241, 236, 236, 0.103);
      border-radius: 18px;
      box-shadow: 0 8px 24px rgba(82, 82, 83, 0.15);
      padding: 2.5rem 2rem;
      max-width: 420px;
      width: 100%;
      text-align: center;
    }
    .success-img {
      width: 80px;
      margin-bottom: 18px;
    }
    .short-url {
      font-size: 1.1rem;
      font-weight: 600;
      color: #2a5298;
      word-break: break-all;
    }
    .qr-code {
      margin: 1.2rem 0 1.5rem 0;
      width: 160px;
      border-radius: 8px;
      border: 1px solid #eee;
      background: #f8f9fa;
      padding: 4px;
    }
    .btn-view {
      background: #28a745;
      border: none;
      font-weight: 600;
      border-radius: 8px;
      padding: 0.75rem 1.5rem;
      font-size: 1rem;
    }
    .btn-view:hover {
      background: #218838;
    }
  </style>
</head>
<body>
  <div class="success-card">
    <img src="https://cdn-icons-png.flaticon.com/512/845/845646.png" alt="Success" class="success-img" />
    <h2 class="mb-3 blod text-success">URL Created Successfully!</h2>
    <div class="mb-2">
      <span class="short-url">Short URL: <a href="<%= shortUrl %>" target="_blank"><%= shortUrl %></a></span>
    </div>
    <div class="mb-3">
      <div class="fw-bold mb-1">QR Code:</div>
      <img src="<%= qrCode %>" alt="QR Code" class="qr-code" />
    </div>
    <a href="/api/url/" class="btn btn-view">View All URLs</a>
  </div>

  <!-- Vanta.js and dependencies -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r121/three.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/vanta@latest/dist/vanta.net.min.js"></script>
  <script>
    document.addEventListener("DOMContentLoaded", function() {
      VANTA.NET({
        el: "body",
        mouseControls: true,
        touchControls: true,
        gyroControls: false,
        minHeight: 200.00,
        minWidth: 200.00,
        scale: 1.00,
        scaleMobile: 1.00,
        color: 0xff3f81,
        backgroundColor: 0x23153c,
        points: 10,
        maxDistance: 20,
        spacing: 15,
        showDots: true
      });
    });
  </script>
</body>
</html>
