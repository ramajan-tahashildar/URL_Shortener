<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL Shortener | Generate</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap');
    body {
      font-family: 'Montserrat', sans-serif;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      /* color: #f5f5f5; */
      /* background: linear-gradient(135deg, #5680c0 0%, #b4bbc7 100%); */
    }
    .shortener-card {
      background: rgba(255, 255, 255, 0.1);
      border-radius: 12px;
      padding: 2rem;
      max-width: 480px;
      width: 100%;
      box-shadow: 0 8px 16px rgba(0,0,0,0.3);
    }
    .shortener-card h2 {
      font-weight: 600;
      margin-bottom: 1.5rem;
      text-align: center;
      color: #fff;
    }
    .form-control {
      border-radius: 8px;
      background: rgba(255,255,255,0.2);
      border: none;
      color: #fff;
    }
    .form-control::placeholder {
      color: rgba(255,255,255,0.7);
    }
    .btn-generate {
      background: #28a745;
      border: none;
      border-radius: 8px;
      padding: 0.75rem;
      font-weight: 600;
      width: 100%;
    }
    .btn-generate:hover {
      background: #218838;
    }
    .result-box {
      margin-top: 1.5rem;
      text-align: center;
      background: rgba(0,0,0,0.3);
      padding: 1rem;
      border-radius: 8px;
    }
    .qr-code {
      margin-top: 1rem;
      width: 120px;
    }
    .view-all-btn {
      margin-top: 1.5rem;
      display: block;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="shortener-card">
    <h2>Shorten Your URL</h2>
    <form method="POST" action="/api/url/">
      <div class="mb-3">
        <input type="url" name="redirectUrl" class="form-control" placeholder="Enter your URL here" required />
      </div>
      <button class="btn btn-generate" type="submit">Generate</button>
    </form>

    <% if (shortUrl && qrCode) { %>
      <div class="result-box">
        <p><strong>Short URL:</strong> <a href="<%= shortUrl %>" target="_blank" class="text-info"><%= shortUrl %></a></p>
        <img src="<%= qrCode %>" alt="QR Code" class="qr-code" />
      </div>
    <% } %>

    <a href="/api/url/getAllUrl" class="btn btn-light view-all-btn">View All URLs</a>
  </div>

  <!-- Vanta.js and dependencies -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r121/three.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/vanta@latest/dist/vanta.net.min.js"></script>
  <script>
    document.addEventListener("DOMContentLoaded", function() {
      VANTA.NET({
        el: "body",
        mouseControls: true,
        touchControls: true,
        gyroControls: false,
        minHeight: 200.00,
        minWidth: 200.00,
        scale: 1.00,
        scaleMobile: 1.00,
        color: 0xff3f81,
        backgroundColor: 0x23153c,
        points: 10,
        maxDistance: 20,
        spacing: 15,
        showDots: true
      });
    });
  </script>
</body>
</html> 